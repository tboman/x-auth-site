{
  "name": "x-auth",
  "version": "1.0.0",
  "private": true,
  "description": "X-Auth marketing site — static HTML served from public/, no build required.",
  "scripts": {
    "build": "echo \"No build step — public/ is served as-is by Firebase Hosting\""
  }
}
